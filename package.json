{
  "name": "webmonitor",
  "version": "1.0.0",
  "description": "play a sound when a change is detected on a website",
  "scripts": {
    "start": "cd temp && WEBMONITOR_HEADLESS=false node ../bin/daemon.js",
    "lint": "npm run eslint && npm run prettier",
    "eslint": "eslint . --fix",
    "prettier": "prettier \"**/*.{js,json,md,yml,yaml}\" --write --loglevel error"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^4.0.0",
    "node-fetch": "^2.6.1",
    "selenium-webdriver": "^4.0.0-beta.1"
  },
  "devDependencies": {
    "eslint": "^7.12.0",
    "eslint-config-prettier": "^7.2.0",
    "prettier": "^2.1.2"
  }
}
